[package]
name = "rpv-proto"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
tracing = "0.1"
arc-swap = "1"
libc = "0.2"

[dev-dependencies]
reed-solomon-erasure = { version = "6", features = ["simd-accel"] }